package com.paevolution.appiogateway;

import java.util.Date;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import com.paevolution.appiogateway.core.domain.Messaggi;

@SpringBootTest
public class MessaggiMapperUnitTest {
    //MessaggiMapper mapper = Mappers.getMapper(MessaggiMapperImpl.class);

    @Test
    void getNewMessageFromMessaggi() {

//	Messaggi source = Messaggi.builder().timeToLive(4200).subject("Messaggio di Test del mapping").markdown(
//		"######## Contenuto del messaggio di test. HTML CSS JAVA Javascrist ####### Hello World C++ e C-- #####$$$$$$$$$$$$$$$$$$$$$$$$$ Lorem Ipsum")
//		.dueDate(new Date()).fiscalCode("AAAAAA00A00A000A").build();
	//NewMessage target = mapper.messaggiToNewMessage(source);
	//assertThat(target).isNotNull();
    }
}
